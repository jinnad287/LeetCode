class Solution {
public:
    int gcdOfOddEvenSums(int n) {
        // sumOdd = n * n;
        // sumEven = n * (n+1);
        // here we can see that the common between is n
        // which is the GCD
        // so gcd(sumOdd, sumEven) = n

        return n; 
    }
};